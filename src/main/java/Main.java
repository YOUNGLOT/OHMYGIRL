import helper.Helper;
import lombok.SneakyThrows;

import java.io.IOException;

public class Main {
    @SneakyThrows
    public static void main(String[] args) {
        //테이블 만들고 아래 돌리면 entity, dao 파일 생성

        //Helper.getInstance().makeEntityAndDaoClass();
        //Helper.getInstance().makeLoadClass();


        //적제 해 볼까?

        /*String folderDirectory = "./data";

        String[] fileNames = Helper.getInstance().getfileNames(folderDirectory);

        for (int i = 0; i < fileNames.length; i++) {

            System.out.println(String.format(" 시작합니당 : %s 파일 시작!", fileNames[i]));

            String fileDirectory = String.format("%s/%s", folderDirectory, fileNames[i]);

            //todo : load함수 넣어요

        }*/
    }
}


